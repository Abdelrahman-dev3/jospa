# ملخص التحديثات (Walkthrough) لربط دفع الموبايل

لقد قمنا بتنفيذ الخطة المعتمدة بالكامل وتحديث الواجهات البرمجية (APIs) المطلوبة لتطبيق الموبايل، والآن أصبح نظام الدفع متوافقاً تماماً وبأعلى معايير الحماية والتكامل:

## 1. معاينة الفاتورة قبل الدفع (Checkout Preview)
أضفنا المسار الجديد `POST /api/cart-checkout-preview` والذي يعيد للمبرمج JSON مفصل يحتوي على:
- `subtotal` و `tax`.
- `total_before_submethods` (الإجمالي قبل الرصيد).
- تفاصيل الخصومات المطبقة (`coupon_discount_amount`, `used_wallet`, `used_loyalty`, `used_gift`).
- `final_amount` (المبلغ النهائي المطلوب دفعه عبر البوابة).
- `is_full_balance_covered` (مؤشر يوضح هل الرصيد غطى الفاتورة أم لا).

## 2. الاستعلام عن محاولة الدفع (Payment Status API)
أضفنا المسار `GET /api/payment-attempt/status/{attempt_id}` والذي يتيح للتطبيق التأكد من حالة الدفع في حال انقطاع الاتصال أو الخروج من التطبيق، حيث يعيد حالة المحاولة ورقم الفاتورة.

## 3. مفتاح منع التكرار (Idempotency Key)
أصبح الـ API `POST /api/cart-pay` يدعم قراءة هيدر `Idempotency-Key`.
إذا قام التطبيق بإرسال نفس الطلب مرتين بنفس المفتاح، سيرد الخادم تلقائياً بالنتيجة السابقة من الـ `Cache` (بدون إنشاء طلب جديد لدى بوابات الدفع)، مما يمنع حدوث خصومات مكررة للعميل.

## 4. إرجاع رقم الفاتورة `invoice_id`
تم تحديث كافة استراتيجيات الدفع (`UrPay`, `Card`, `Tabby`, `Tamara`) بحيث إذا دفع العميل الفاتورة كاملة عبر المحفظة أو النقاط، فإن الاستجابة ترجع له رقم الفاتورة `invoice_id` ليعرضه فوراً في شاشة التأكيد في التطبيق.

---
> [!TIP]
> يرجى تمرير هذه التحديثات مع الهيدرز إلى مطور التطبيق لكي يبدأ الاختبار (Testing) مباشرة. جميع التعديلات محفوظة وجاهزة للاستخدام في بيئة العمل.
